# Plan: Complete Modular Architecture Migration

## Current State (60% Complete)

| Component | Status |
|-----------|--------|
| Core infrastructure (`src/core/`) | ✅ Modular |
| New modules (san-pham, danh-muc, don-hang, test) | ✅ Modular |
| Legacy modules | ⚠️ Mixed structure |
| `src/services/` | ❌ Legacy layer |
| `src/core/index.ts` | ❌ Missing barrel export |

---

## Work Items

### 1. Create src/core/index.ts Barrel Export
**Priority:** High | **Effort:** Low

```typescript
export * from './types/base.types';
export * from './service/base.service';
export * from './hooks/base.hooks';
export * from './utils';
export * from './config/api.config';
```

### 2. Remove Legacy src/services/
**Priority:** High | **Effort:** Low

- Remove entire `src/services/` directory
- It's demo code from ant-design-pro, not used by business modules
- Already verified: business modules (san-pham, danh-muc, don-hang) use `src/core/`

### 3. Legacy Modules Decision
**Priority:** Info | **Effort:** N/A

| Module Type | Action |
|-------------|--------|
| Business modules (san-pham, danh-muc, don-hang) | ✅ Already complete |
| UI-only modules (dashboard, auth, form, list, profile, result, table, exception, user) | Keep as-is, minimal change needed |

UI-only modules don't have business logic - their page-level "service.ts" files are acceptable for simple API data display. No migration needed.

---

## Proposed Folder Structure (Target)

```
src/
├── core/                    # Shared infrastructure ✅
│   ├── index.ts            # NEW: barrel export
│   ├── config/api.config.ts
│   ├── hooks/base.hooks.ts
│   ├── service/base.service.ts
│   ├── types/base.types.ts
│   └── utils/
├── modules/                 # Feature modules
│   ├── san-pham/           # ✅ Complete modular
│   ├── danh-muc/           # ✅ Complete modular
│   ├── don-hang/           # ✅ Complete modular
│   └── <new-module>/       # Each self-contained:
│       ├── index.ts        # Barrel export
│       ├── types/
│       ├── service/
│       ├── hooks/
│       ├── components/
│       └── pages/
└── pages/                  # For Ant Design Pro routes
```

### REMOVED:
- `src/services/` - delete entirely

---

## Proposed Folder Structure (Target)

```
src/
├── core/                    # Shared infrastructure
│   ├── index.ts            # NEW: barrel export
│   ├── config/api.config.ts
│   ├── hooks/base.hooks.ts
│   ├── service/base.service.ts
│   ├── types/base.types.ts
│   └── utils/
├── modules/                 # Feature modules
│   ├── san-pham/           # ✅ Complete modular
│   ├── danh-muc/           # ✅ Complete modular
│   ├── don-hang/           # ✅ Complete modular
│   └── <module>/           # Each self-contained:
│       ├── index.ts        # Barrel export
│       ├── types/
│       ├── service/
│       ├── hooks/
│       ├── components/
│       └── pages/
├── pages/                  # Only for Ant Design Pro routes that don't fit modular
└── services/               # REMOVE after verifying no deps
```

---

## Migration Steps

### Step 1: Create core barrel export
- Create `src/core/index.ts`
- Export: types, service, hooks, utils, config

### Step 2: Remove legacy services
- Delete `src/services/` directory entirely
- These are demo-only from ant-design-pro original template

### Step 3: Verify modular modules
- Confirm san-pham, danh-muc, don-hang, test have full modular structure
- Check each has: types/, service/, hooks/, pages/, index.ts

### Step 4: Test module generation
- Run: `npm run generate:module test-migration`
- Verify generated structure matches target

---

## Verification Checklist

- [ ] `src/core/index.ts` exists and exports everything
- [ ] `src/services/` deleted
- [ ] Business modules (san-pham, danh-muc, don-hang, test) have complete modular structure
- [ ] All modules have barrel export `index.ts`
- [ ] `npm run generate:module` produces correct structure
- [ ] TypeScript compiles without errors

---

## Open Questions

~~1. ~~src/services/ant-design-pro/~~: CONFIRMED - can be removed
~~2. ~~Legacy modules~~: CONFIRMED - only migrate business modules, UI-only can keep current structure
